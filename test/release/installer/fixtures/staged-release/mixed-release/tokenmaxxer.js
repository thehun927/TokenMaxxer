// Fixture: staged release server plugin bundle (MIXED RELEASE).
// release-tag: v0.2.0
// release-commit: fedcba9876543210fedcba9876543210fedcba98
// This payload embeds identity from a DIFFERENT release tag (v0.2.0) than
// RELEASE.json (v0.1.0). A release set must never mix payloads from two tags.
export const fixture = "tokenmaxxer.js from v0.2.0"
